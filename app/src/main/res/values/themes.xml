<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MeleeTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/mercado_yellow</item>
    </style>

    <style name="Theme.MeleeTest" parent="Base.Theme.MeleeTest" />

    <!-- Splash Activity theme. -->
    <style name="Base.Theme.MeleeTest.Splash" parent="Theme.SplashScreen">
        <!-- Set the splash screen background to black -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <!-- Set the theme of the Activity that follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/Base.Theme.MeleeTest</item>
    </style>
</resources>